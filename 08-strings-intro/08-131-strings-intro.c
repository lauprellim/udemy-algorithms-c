#include <stdio.h>
#include <string.h>

int main() {

  char myString0[20];

  printf("Enter a string without a space: ");
  scanf("%s", myString0);

  printf("\nString is: %s\n", myString0);
  return 0;
}
