#include<stdio.h>

// tree recursion is a kind of recursion that calls itself more
// than one time.

void fun(int n) {
  if (n>0) {
    printf("%d ",n);
    fun(n-1);
    fun(n-1);
  }
}

int main() {
  fun(4);
  printf("\n");
  return 0;
}
