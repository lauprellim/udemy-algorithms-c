#include <stdlib.h>
#include <stdio.h>

// create array of size 5 in heap memory

int main() {
    printf("Data type char consumes %d byte.\n", sizeof(char));
    printf("Data type int consumes %d bytes.\n", sizeof(int));
    printf("Data type float consumes %d bytes.\n", sizeof(float));
    printf("Data type double consumes %d bytes.\n", sizeof(double));        
    return 0;
}