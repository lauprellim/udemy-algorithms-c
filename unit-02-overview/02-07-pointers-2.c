#include <stdlib.h>
#include <stdio.h>

// create array of size 5 in heap memory

int main() {
int *p;

// allocate 5 integers of size "int" (compiler takes care of the sizeof(int))
p = (int * ) malloc(5*sizeof(int));



}